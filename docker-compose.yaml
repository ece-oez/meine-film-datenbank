version: "3.9"

services:

  backend:
    build:
      context: ./backend
    container_name: filmdb-backend

    ports:
      - "8080:80"

    volumes:
      - ./backend:/var/www/html

    depends_on:
      - mariadb

    environment:
      DB_HOST: mariadb
      DB_NAME: filmdb
      DB_USER: filmdb
      DB_PASSWORD: secret

  mariadb:
    image: mariadb:11

    container_name: filmdb-db

    restart: unless-stopped

    environment:
      MYSQL_ROOT_PASSWORD: root
      MYSQL_DATABASE: filmdb
      MYSQL_USER: filmdb
      MYSQL_PASSWORD: secret

    ports:
      - "3306:3306"

    volumes:
      - db_data:/var/lib/mysql
      - ./database:/docker-entrypoint-initdb.d

  phpmyadmin:
    image: phpmyadmin:latest
    container_name: filmdb-phpmyadmin

    restart: unless-stopped

    depends_on:
      - mariadb

    environment:
      PMA_HOST: mariadb
      PMA_PORT: 3306

    ports:
      - "8081:80"

volumes:
  db_data: